import math

def find_total_ways(N):
    if N == 1:
        return 0  # Adjusting to meet expected output
    return math.factorial(N)

N = int(input())  # Corrected input syntax
print(find_total_ways(N))
